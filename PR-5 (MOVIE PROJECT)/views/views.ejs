<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DK Cinema</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: "Rubik", sans-serif;
            background: linear-gradient(135deg, #1e3a8a, #2b2d42);
            margin: 0;
            padding: 20px;
            color: #edf2f4;
        }

        .card {
            background-color: #3d405b;
            color: #edf2f4;
            border: none;
            border-radius: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.25);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.4);
        }

        .card img {
            border-top-left-radius: 10px;
            border-top-right-radius: 10px;
        }

        .btn-custom {
            background: linear-gradient(135deg, #d90429, #ef233c);
            color: #edf2f4;
            border: none;
            transition: background 0.3s ease, transform 0.2s ease;
        }

        .btn-custom:hover {
            background: linear-gradient(135deg, #ef233c, #d90429);
            transform: scale(1.05);
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="text-center my-4">DK Cinema</h1>
        <div class="row g-4">
            <% users.map((val, i) => { %>
            <div class="col-md-4 col-sm-6">
                <div class="card h-100">
                    <img src="<%= val.image %>" class="card-img-top" alt="<%= val.name %>">
                    <div class="card-body">
                        <h5 class="card-title"><%= val.name %></h5>
                        <p class="card-text"><strong>Price:</strong> $<%= val.price %></p>
                        <p class="card-text"><%= val.desc %></p>
                    </div>
                    <div class="card-footer d-flex justify-content-between">
                        <a href="/delete?id=<%= val.id %>" class="btn btn-custom">Delete</a>
                        <a href="/edit?id=<%= val.id %>" class="btn btn-custom">Edit</a>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
