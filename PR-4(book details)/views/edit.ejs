<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Edit Page</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f0f4f8;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .form-container {
            background: #ffffff;
            border-radius: 8px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            padding: 40px;
            max-width: 450px;
            width: 100%;
            text-align: center;
        }

        h2 {
            font-weight: 600;
            font-size: 1.8rem;
            margin-bottom: 30px;
            color: #333;
        }

        .form-label {
            font-weight: 500;
            text-align: left;
            display: block;
            margin-bottom: 5px;
            color: #555;
        }

        .form-control {
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
            font-size: 1rem;
            transition: border-color 0.3s ease-in-out;
        }

        .form-control:focus {
            border-color: #007bff;
            box-shadow: none;
        }

        .form-select {
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
            font-size: 1rem;
            transition: border-color 0.3s ease-in-out;
        }

        .form-select:focus {
            border-color: #007bff;
            box-shadow: none;
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            padding: 12px 0;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            width: 100%;
            transition: background-color 0.3s ease;
        }

        .btn-custom:hover {
            background-color: #0056b3;
        }

        .view-details {
            display: inline-block;
            margin-top: 20px;
            color: #007bff;
            font-weight: 500;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .view-details:hover {
            color: #0056b3;
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="form-container">
        <h2> Edit Page</h2>
        <form action="/updateRecord" method="post">
            <input type="hidden" name="editid" value="<%= data.id %>">

            <div class="mb-3">
                <label for="name" class="form-label">Book Name</label>
                <input type="text" class="form-control" id="name" name="name" value="<%= data.name %>"
                    placeholder="Enter book name">
            </div>

            <div class="mb-3">
                <label for="price" class="form-label">Book Price</label>
                <input type="text" class="form-control" id="price" name="price" value="<%= data.price %>"
                    placeholder="Enter book price">
            </div>

            <div class="mb-3">
                <label for="page" class="form-label">Book Pages</label>
                <select class="form-select" id="page" name="page">
                    <option>-- Select pages --</option>
                    <% const pages=["100 to 200", "200 to 300" , "300 to 400" , "400 to 500" ]; pages.map((p)=> { %>
                        <option <% if(data.pages==p) { %> selected <% } %> value="<%= p %>"><%= p %></option>
                    <% }) %>
                </select>
            </div>

            <div class="mb-3">
                <label for="author" class="form-label">Book Author</label>
                <input type="text" class="form-control" id="author" name="author" value="<%= data.author %>"
                    placeholder="Enter author name">
            </div>

            <div class="d-grid">
                <button type="submit" class="btn-custom">Submit</button>
            </div>
        </form>
        <a href="/" class="view-details"> View Page</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
