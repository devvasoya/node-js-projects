<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Blog</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #ffafbd, #ffc3a0);
        }
        h1 {
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
        .card {
            border: none;
            border-radius: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }
        .btn-primary {
            background-color: #6a11cb;
            background-image: linear-gradient(to right, #6a11cb, #2575fc);
            border: none;
        }
        .btn-primary:hover {
            background-image: linear-gradient(to right, #2575fc, #6a11cb);
        }
        .btn-secondary {
            background-color: #6a11cb;
            background-image: linear-gradient(to right, #6a11cb, #2575fc);
            border: none;
        }
        .btn-secondary:hover {
            background-image:linear-gradient(to right, #2575fc, #6a11cb);
        }
    </style>
</head>
<body>
    <div class="container py-5">
        <div class="text-center mb-4">
            <h1>Welcome to Edit Product </h1>
        </div>

        <div class="card mx-auto" style="max-width: 600px; background: #fff;">
            <div class="card-body">
                <form action="/product/updateproduct" method="post" enctype="multipart/form-data">
                    <input type="text"  value="<%= product.id %>" name="editid">
                    <div class="mb-3">
                        <label for="blogename" class="form-label">product Name:</label>
                        <input type="text" class="form-control" id="blogename" value="<%= product.productName %>" name="productName" required>
                    </div>
                    <div class="mb-3">
                        <label for="description" class="form-label">Blog Description:</label>
                        <input class="form-control" id="description" name="description" value="<%= product.description %>" rows="4" required/>
                    </div>
                    <div class="mb-3">
                        <label for="blogename" class="form-label">product Price:</label>
                        <input type="text" class="form-control" id="blogename" value="<%= product.price %>"name="price" required>
                    </div>
                    <div class="mb-3">
                        <label for="blogename" class="form-label">product Quantity:</label>
                        <input type="text" class="form-control" id="blogename" value="<%= product.qty %>" name="qty" required>
                    </div>
                    <div class="mb-3">
                        <label for="image" class="form-label">Image:</label>
                        <input type="file" class="form-control" id="image" name="image"/>
                        <img src="../<%= product.image %>" style="width: 100px;margin-top: 10px;" name="image" class="card-img rounded-0" alt="">
                    </div>
                    <div class="d-grid">
                        <button type="submit" class="btn btn-primary">Submit</button>
                    </div>
                </form>
            </div>
        </div>

        <!-- <div class="text-center mt-4">
            <a href="/adminview" class="btn btn-secondary">Go to Dashboard</a>
        </div> -->
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
